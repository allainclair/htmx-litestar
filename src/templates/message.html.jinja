{#<div hx-put="/users/messages/submit" hx-target="#button-text" hx-trigger="load" hx-swap="outerHTML" id="user-message" class="max-w-md mx-auto mt-8">#}
<div id="user-message" class="max-w-md mx-auto mt-8">
		<div class="bg-gray-800 rounded-md shadow-md p-4 text-white">
			<div class="font-bold text-lg mb-2">User's Message</div>
			<div class="mb-2"><span class="font-semibold">User:</span> {{ user.name }}</div>
			<div class="mb-2"><span class="font-semibold">Email:</span> {{ user.email }}</div>
			<div><span class="font-semibold">Message:</span> {{ user.message }}</div>
		</div>
</div>
